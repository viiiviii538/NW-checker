[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = src
addopts = -ra -q
markers =
    benchmark: performance benchmark
    fastapi: FastAPI関連
    nmap: nmap関連
    slow: 長時間/外部依存
asyncio_mode = auto
